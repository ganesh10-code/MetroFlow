{
  "name": "metroflow",
  "version": "1.0.0",
  "description": "MetroFlow - AI-Based Train Induction Planning System",
  "scripts": {
    "backend": "venv\\Scripts\\python -m uvicorn --app-dir backend app.main:app --reload --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx -y concurrently \"npm run backend\" \"npm run frontend\"",
    "start": "npm run backend"
  }
}
